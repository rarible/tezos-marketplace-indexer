spec_version: 1.2

datasources:
  tzkt:
    kind: tzkt
    url: ${TZKT_URL:-https://api.ghostnet.tzkt.io}
    http:
      connection_timeout: 60
  metadata:
    kind: metadata
    url: https://metadata.dipdup.net
    network: ithacanet
  ipfs:
    kind: ipfs
    url: https://ipfs.io/ipfs

hooks:
  process_metadata_for_tokens:
    callback: process_metadata_for_tokens
    atomic: False
  process_metadata_for_collections:
    callback: process_metadata_for_collections
    atomic: False
  process_royalties:
    callback: process_royalties
    atomic: False

jobs:
  process_metadata_for_tokens:
    hook: process_metadata_for_tokens
    crontab: "* * * * *"
  process_metadata_for_collections:
    hook: process_metadata_for_collections
    crontab: "* * * * *"
  process_royalties:
    hook: process_royalties
    crontab: "* * * * *"

advanced:
  rollback_depth: 2
  early_realtime: True
  metadata_interface: True
  reindex:
    manual: wipe
    migration: exception
    rollback: ignore
    config_modified: ignore
    schema_modified: ignore

custom:
  enabled: ${KAFKA_ENABLED:-false}
  kafka_address: ${KAFKA_ADDRESS:-kafka:9092}
  client_id: dipdup-rarible-${APPLICATION_ENVIRONMENT:-mainnet}-producer
  kafka_security_protocol: ${KAFKA_SECURITY_PROTOCOL:-SASL_PLAINTEXT}
  sasl:
    mechanism: ${KAFKA_SASL_MECHANISM:-PLAIN}
    username: ${KAFKA_USERNAME:-rarible}
    password: ${KAFKA_PASSWORD:-changeme}
  run_fix_jobs: False